# 2026-08-31

## gph2ccm 复评 + 四阶段开发规划（commit `0f53e5b`）

8 项缺口收尾后复评功能完整度/深度，产出下一步规划写入
`gph2ccm_functional_assessment.md` §6（已推 origin/main）：

- **现状**：21 项能力 = 已实现 12 / 部分 7 / 缺失 2（多 processor、高阶单元）。
  核心网格层完整且经 330 万单元验证；物理/求解层处于「描述性元数据载体」阶段。
- **新识别的深度短板**：`gph2ccm.*` 元数据**只写不读**——仓库内没有官方
  途径读回（只有 gitignored 的 `tools/nodestr_probe.py` 探针），README 导入后
  清单只能手工对照。
- **规划四阶段**：
  - A 加固：M4 try/finally、M1 chunk-vertices 处置、L3 体积加权质心、H2 max_id 核对
  - B 元数据闭环：`python -m gph2ccm inspect`（读回报告）、Java 宏生成器
    （半自动设置 MRF/周期/BC）、regions JSON schema 校验、诊断分级
  - C 求解就绪：C1 PeriodicBoundaries 写入（前置=周期对几何匹配）、
    C2 初始场（前置=GPH 侧有无场数据，需先做可行性结论）、C3 多 processor
  - D 工程化：self-hosted CI 跑全量 + ImportCcmCheck.java、版本行为对照表、性能基线
- **推荐顺序**：A1 → A2 → B3 → B1 → B4 → A3/A4 → B2 → C1 → C2/C3 视前置结论。
  B1/B3 投入产出比最高（把「说明书」变成工具）。

代码核对结论：M4（convert.py 无 finally）、M1（chunk_vertices 参数仍在但
2D 数组已单次写入）均确认未修，列入阶段 A。
