# 2026-09-04

## 任务列表清理（用户指令：① 清理收尾）

- #2 边界条件结构化 ✅：规范化 + `gph2ccm.BC.*`/BCKeys + C1 生效周期对；求解级走 B2 宏。
- #4 周期/滑移配对 ✅：C1 几何校验 + `InterfaceDefinitions`（PeriodicInterface）生效写入。
- #6 2D 包裹 ✅（范围决策）：检测 + `gph2ccm.Note.*` 自说明；不挤壳层（#18 ❌ 为正确状态）。
- #7 质量修复 ✅（范围决策）：`gph2ccm.Qual.*` 摘要 + diagnose_quality 分级 + topo_check；不改网格。
- 保留 pending（有意为之）：#3 MRF（B2 宏为现行方案，legacy CCM 无求解级 MRF 语义）、#5 多 processor（C3 靠后：需分区算法、导入侧收益未证实）。
- #26 D1 仍 in_progress：等用户在 STAR-CCM+ 机器注册 runner（docs/self_hosted_ci.md 一键脚本）。
- 任务列表终态：23 completed / 2 pending（#3、#5，均为设计内保留）/ 1 in_progress（#26 用户侧动作）。

## 真实大网格 FPH→CCM 全流程导入验证（M2 + C2 终验）

- 输入 `laptop_thermal_steady_scaled_v3_10.fph`（1.36 GB，Cradle scFLOW 网格+结果）。
- 转换：FPH 解析 104.9s（mmap）→ 写出 22.1s（6,831,117 cells / 7,723,969 verts / 21,389,752 faces，10 cell types / 3 边界区 / **10 个解算场**）→ verify OK；端到端 **2m41s**，输出 1330.5 MB。
- 场读回比对：16 变量 min 逐场精确一致；7 变量含 1e20 哨兵 → CCM 侧全清零（PRES max 1e20→68.16、TURK→0.00768 等）。
- STAR-CCM+ 20.02.007-R8 batch 导入（C:/sc8 启动器，license ccmpsuite）：**IMPORT_DONE**，3m17s；air_domain 6,183,269 + case2 647,848 = 6,831,117 精确一致；open/impeller1_s/impeller2_s = 245,612 一致；导入器自动创建 Interface-1-2（两侧各 516,906 面，fluid/solid 共轭界面）。
- wmic.exe 被沙箱拦截（行为表 #11），发生在宏完成后，不影响结果。
- 文档：performance_baseline.md 新增「真实 FPH 端到端记录」节（6.8M 回归参照）；manual_verification.md 新增「验证记录」节。
- 遗留产物：`out_laptop_v3.ccm`（1.33 GB，D 盘 99% 满，14G 剩余）——待用户决定保留（GUI 查看）或删除。
