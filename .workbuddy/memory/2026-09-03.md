# 2026-09-03

## D1 文档细化（self-hosted CI 注册手册可照抄化）

- 重写 `docs/self_hosted_ci.md`，把 runner 注册步骤细化为「可直接照抄」级别：
  - 新增「前置条件」与「第 0 步取一次性 token」澄清（注册 token ≠ PAT，约 1 小时失效）。
  - 锁定 runner 版本 v2.337.0（`actions/runner/releases/latest` 实测），给出完整 PowerShell 下载/解压/配置/装服务命令 + 一键脚本。
  - 澄清标签：只传 `--labels windows,starccm`，`self-hosted` 由 GitHub 自动附加（避免重复）。
  - 记录 ccmio.dll 自动发现完整规则（来自 `gph2ccm/ccmio.py::find_ccmio_library`），说明 full-suite 无需 secret。
  - 补充本机实测无空格布局 `C:\sc8\star\bin\starccm+.bat`、license `C:\Program Files\Siemens\license.dat`，作为 `STARCCM_BIN` 示例。
  - 新增「验证清单」与「常见问题」。
- 本机关键事实（供 D1 后续闭环参考）：
  - 仓库 HTTPS：`https://github.com/BHCLLBHCLL/gph2ccm`（SSH remote `git@github.com:BHCLLBHCLL/gph2ccm.git`）。
  - STAR-CCM+ 安装：`C:\Program Files\Siemens\20.02.007-R8\STAR-CCM+20.02.007-R8`。
  - 最新 GitHub Actions runner：v2.337.0。
- D1 仍为「就绪待用户注册」，代码/文档全部完成。

## C2 第一切片：真实场数据写入（下午/晚间）

- 用户指令：手动验证写入文档 + 继续别的方向。
- **`docs/manual_verification.md`（新增，commit `24e3f0a`）**：M1–M7 手动验证清单（runner 注册/batch 导入/GUI 检查/周期界面生效/宏数值确认/性能基线复核/版本升级复核），self_hosted_ci.md 与评估文档已交叉引用。
- **C2 第一切片（真实 cell 场数据写入）**：
  - `model.py`：`SolutionField` 数据类 + `_normalize_solution_fields` fail-fast 校验（名称≤32、短名≤8 截断、数据形状、仅 cell 位置）+ `build_model(solution_fields=)` + `CcmModel.solution_fields`。
  - `ccmio.py`：绑定 6 个 Field API（NewField/ReadField/WriteFieldDataf/ReadFieldDataf/WriteConstantFieldDataf/Write+ReadMultiDimensionalFieldData）+ 7 个包装方法；向量分量读回用 `read_vector_component`。
  - `convert.py`：`_write_solution_fields`（FieldSet→FieldPhase→Field→FieldData，向量按官方 writeexample.cpp 拆 X/Y/Z 标量分子场 + MultiDimensional 链接，短名占末位）挂 `write_processor(solution=…)`；`convert_model(solution_fields=)`。
  - 关键坑：FieldData 读回必须 `CCMIONextEntity`（GetEntity 不可靠）；Read 变体绑定最初漏掉。
  - 测试：`test_solution_fields_roundtrip` + `test_solution_fields_validation`，**29 passed 0 failed**。
  - 1M 单元冒烟：4 场 +16 MB（=4×4B×1M 精确吻合）、write 3.32s（场写入 ~2.2s）、读回字段 Pressure/Velocity/X/Y/Z 正确。
  - 文档同步：README（范围声明例外 + API 用法）、评估文档（矩阵 #13/#19→✅、#1 进度→✅、C2 行、结论）、version_behavior_table #16（Field 写入配方）。
- 教训：对同一文件的并行 Edit 会互相覆盖（第一个 edit 静默丢失）——同文件编辑必须串行。
